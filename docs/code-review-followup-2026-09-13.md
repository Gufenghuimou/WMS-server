# 清理复查与现有功能修复（2026-09-13）

本次基于当前工作区复查，保留已有修改；未修改实际数据库、删除文件或提交 Git。ERP、BOM、工单和生产看板按用户确认继续保留 TODO。

## 已确认的清理和既有修复

- 7 个旧模板已删除，当前模板引用有效。
- 旧注释路由已清理，PC 壳与库存卡片重复表单已移除；组合检查未发现当前 PC 壳、公共弹窗与各页面之间的重复 HTML ID。
- 资产盘点提交已使用 `/api/asset_audit/commit`。
- SIM 卡编辑和生产接口已补权限依赖。
- 前端资产编号采用字符串递增；批量提交检查本批和数据库重复，提交后才安排打印。
- 重复路由配置、主要重复全局函数已清理；出库校验已接入。

## 本次修复

| 问题 | 修改 |
| --- | --- |
| 资产备注 HTML 已改 ID，JS 仍读取库存备注 | `global_modal.js` 改为读取 `editAssetRemarks` |
| PN2 查询命中字段不匹配 | 资产入库、重打标签使用接口实际返回的 `match_type` |
| Escape 关闭弹窗后 Promise 不结束 | Alert、Confirm、Info 关闭统一通过按钮回调结束等待；Confirm 关闭返回 false |
| 重复扫码弹窗引用另一文件的局部音效变量 | 将 noticeAudio 放到实际使用它的公共弹窗作用域 |
| 出库校验失败后提交按钮持续禁用 | 校验移到禁用按钮之前 |
| 切换语言后后台用户表单失去异步事件 | 重绘后重新绑定表单事件 |
| 报废清单新增、删除后切语言恢复旧数据 | 同步 ASSET_SCRAP_DATA，保留停用标记，删除后重绘空状态 |
| 点击申请日志、申请队列标签内部元素切换错误 | 使用 closest 定位标签容器 |
| 出库备注未写入日志 | 接收 note，去除首尾空格；空白时仍记 OUT |
| 库存编辑响应缺 remarks | 响应补 remarks，避免前端缓存保留旧值 |
| SIM 启停响应遗漏被重置的字段 | 补 is_stock、direct_user、project |
| 加载提示翻译键缺失及拼写错误 | 复用现有提交/处理中翻译，修正 scan_area_note、data-i18n-placeholder |
| 静态代码、页面和翻译缓存 30 天 | JS/CSS/HTML/JSON 使用 no-cache 进行条件验证；其他资源保持原策略 |
| PC 后台脚本重复加载 | 移除壳中提前加载的 backend.js，由路由加载；移除 PC 不使用的扫码脚本引用 |
| 生产统计范围与返回值错误 | months=11，包含起始月并排除未来月，使用 record.year_month |
| README 初始化说明无效 | 说明启动时自动初始化，修正默认 HTTP 地址和端口 |

已有浏览器缓存可能仍持有旧的 30 天策略，部署后首次需要强制刷新一次。

## 仍有的废弃代码和候选文件（仅记录）

- `middlewares.py` 仍保留已注释的 pending_count 查询及对应无用导入。
- `templates/index.html` 仍有已注释的旧脚本、样式引用。
- AST 检查仍发现无引用导入：dependencies.py、main.py、middlewares.py、utils.py 及 routers 下各模块。`production.py`、`request.py` 的数量较多，可单独清理；本次不因静态结果批量删除导入。
- `static/.DS_Store`、`static/._.DS_Store` 仍在。
- `static/sound/success.mp3`、`static/sound/error.mp3` 没有当前源码引用。
- 数据库只读比对后的孤立图片候选：`static/item_images/1265.jpg`、`155.jpg`、`195.jpg`、`804.jpg`；`static/asset_images/4641-0DHU000.jpg`。删除前仍需确认是否留作备份。
- `fix_db.py` 仍是无运行时调用的一次性脚本，包含删除 user 表的操作；未运行。
- 文档、数据库、证书、日志、依赖和版本控制目录不作为无用文件处理。

## 数据问题：唯一约束尚不能直接添加

只读查询发现资产表无索引，并存在以下重复管理编号：

| ctrl_no | 条数 | 相关资产 ID |
| --- | ---: | --- |
| CC2406120120 | 2 | 541、7293 |
| CC2406120121 | 2 | 542、7294 |
| JPE1901560 | 2 | 2210、7178 |
| nan | 122 | 多条记录，需核对原始导入资料 |

不能自行判断这些资产应删除、合并还是重新编号，因此未改历史记录，也未添加会因这些数据而失败的唯一约束。当前批量接口的查询校验仍不能替代数据库唯一约束的并发保障；需先解决上述数据，再添加约束。

## 验证范围

- 自有 Python AST、JavaScript 语法和翻译 JSON 检查。
- 当前源码中的字面量静态资源引用未发现缺失。
- 模拟测试通过：弹窗关闭及 Escape 的 Promise 结果、重复扫码弹窗初始化、队列/日志标签子元素点击、报废清单增删后语言重绘。
- 隔离接口测试通过：出库备注、编辑备注响应、SIM 状态重置响应、生产统计返回结构与日期查询参数。
- 当前 Python 缺少项目依赖，未启动完整 FastAPI 服务；模拟测试不代表真实数据库事务或浏览器端到端验证。数据库检查全部以 mode=ro 执行。
